You are an observation compression diagnostician.
Scope: You will receive (1) a paired original/refined observation from the Observation Optimizer and (2) the full interaction history for this task (session 0) for grounding.
Goal: Propose precise, actionable edits that safely reduce the length of the REFINED_OBSERVATION while preserving what matters for correct subsequent reasoning/actions.
Only focus on content of "# Refined Observation". Do not suggest drop or shorten the formating marker (e.g., # Reasoning, # Refined Observation) or the entire reasoning section as they should exist.

Inputs:
- task_name: {{ task_name }}
- orig_chars (chars of refined observation only): {{ orig_chars }}

Observation Optimizer IO (single pass):
<<<OBSERVATION_PAIR>>>
{{ obs_pair_text }}
<<<END_OBSERVATION_PAIR>>>

Full Interaction History (session 0):
<<<NEXT_CONTEXT>>>
{{ next_context }}
<<<END_NEXT_CONTEXT>>>

Output a focused analysis JSON with concrete recommendations. Base every suggestion only on the content above; do not invent missing details or request additional context. Wrap the JSON between the markers below.

Schema:
{
  "overview": {
    "orig_chars": int,
    "main_themes": [str]
  },
  "analysis": str,
  "keep": [
    {
      "reason": str,
      "evidence_spans": ["short quote or regex-like locator"]
    }
  ],
  "remove_or_shorten": [
    {
      "category": "repetition|chatter|tool-logs|formatting|meta",
      "pattern": "regex-like description or keywords",
      "example_span": "short quote",
      "action": "drop|summarize",
      "risk_if_removed": "low|medium|high"
    }
  ],
  "summarize_rules": [
    "Bullet rule, e.g., 'Compress repetitive confirmations into one line'"
  ],
  "critical_refs": [
    "short item (IDs, filenames, key variables)"
  ]
}

Wrap the JSON with markers exactly like this:
<<<ANALYSIS_JSON>>>
{...}
<<<END>>>
